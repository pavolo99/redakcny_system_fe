build:
  image: docker:latest
  stage: build
  services:
    - docker:dind
  script:
    - docker login -u $CI_REGISTRY_USER -p $CI_REGISTRY_PASSWORD $CI_REGISTRY
    - docker build -t $CI_REGISTRY/redakcny_system_fe:latest .
    - docker push $CI_REGISTRY/redakcny_system_fe:latest
